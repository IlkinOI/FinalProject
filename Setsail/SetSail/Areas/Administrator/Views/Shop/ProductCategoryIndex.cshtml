@model IEnumerable<SetSail.Models.ProductCategory>

@{
    ViewBag.Title = "ProductCategoryIndex";
    Layout = "~/Areas/Administrator/Views/Shared/_Layout.cshtml";
}

<h2>Product Category Index</h2>

<p>
    @Html.ActionLink("Create New", "ProductCategoryCreate")
</p>
<table class="table table-responsive">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "ProductCategoryUpdate", new { id = item.Id }) |
            <a href="@Url.Action("ProductCategoryDelete","Tour", new { id = item.Id })" onclick="return confirm('Are you sure?')">Delete</a>
        </td>
    </tr>
}

</table>
